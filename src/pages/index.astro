---
import { getEntryBySlug } from "astro:content";
import HeroLayout from "../layouts/HeroLayout.astro";

const page = await getEntryBySlug("pages", "index");
const { Content } = await page.render();
---

<HeroLayout title={page.data.title}>
  <Content />
</HeroLayout>
